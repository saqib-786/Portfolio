import { styled } from "@mui/material";
import { typography } from "@mui/system";

const H2Typo = styled(typography)((theme)=>({
    fontFamily:'Poppins',
    fontSize:'18px',
    fontWeight:500

}))

export default H2Typo